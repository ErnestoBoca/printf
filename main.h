#ifndef MAIN_H
#define MAIN_H
#include <stdarg.h>
#include <stdio.h>

#define BUFFER_SIZE 1024
int _putchar(char c);
int _strlen(char *s);
void _puts(char *str);
int _printf(const char *format, ...);

#endif
